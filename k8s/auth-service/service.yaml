apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: library-app
  labels:
    app: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
